# Environment template for future local services. No variables are required yet.
# Add documented configuration when Docker services are implemented.
# Never commit real credentials or a populated .env file.
